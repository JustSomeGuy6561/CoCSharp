<Application x:Class="CoCWinDesktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CoCWinDesktop"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
		<Style x:Key="CoCButton" TargetType="{x:Type Button}">
			<Setter Property="Width" Value="150" />
			<Setter Property="Height" Value="40" />
			<Setter Property="FontSize" Value="18" />
			<Setter Property="FontFamily" Value="/CoCWinDesktop;component/resources/#Shrewsbury-Titling" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type Button}">
						<Grid>
							<Rectangle Name="bg" Fill="{TemplateBinding Background}"></Rectangle>
							<ContentPresenter Margin="0,5,0,0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
						</Grid>
						<ControlTemplate.Triggers>
							<Trigger Property="IsEnabled" Value="False">
								<Setter Property="Opacity" Value="0.4" />
							</Trigger>
							<MultiTrigger>
								<MultiTrigger.Conditions>
									<Condition Property="IsMouseOver" Value="True" />
									<Condition Property="IsEnabled" Value="True" />
								</MultiTrigger.Conditions>
								<Setter TargetName="bg" Property="Opacity" Value="0.5" />
							</MultiTrigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
		<Style x:Key="CoCText" TargetType="{x:Type TextBlock}" >
			<Setter Property="Foreground" Value="{Binding Path=(local:Settings.FontColor), Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
		</Style>
		<Style x:Key="TextZone" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource CoCText}">
			<Setter Property="Canvas.Left" Value="208" />
			<Setter Property="Canvas.Top" Value="52" />
			<Setter Property="Width" Value="769" />
			<Setter Property="Height" Value="602" />
			<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled" />
			<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
			<Setter Property="TextWrapping" Value="Wrap" />
			<Setter Property="Background" Value="{Binding Path=(local:Settings.TextBackground), Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
			</Setter>
		</Style>
		<Style x:Key="CoCCanvas" TargetType="{x:Type Canvas}" >
			<Setter Property="Width" Value="1000" />
			<Setter Property="Height" Value="800" />
			<Setter Property="Background">
				<Setter.Value>
					<!-- This is some next level magic right here. a forewarning, it'll appear to not work in designer, but appears when you run it.-->
					<ImageBrush ImageSource="{Binding Path=(local:Settings.BackgroundImage), Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
				</Setter.Value>
			</Setter>
		</Style>
	</Application.Resources>
</Application>
